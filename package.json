{
  "name": "zenith",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Zenith — a tiny, joyful habit tracker. Built end-to-end by an autonomous Claude SDLC pipeline.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
